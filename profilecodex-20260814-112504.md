# Codex 会话耗时分析（2026-08-14 11:25:04 +08:00 快照）

分析对象：活动会话 `019ffa80-72c3-7a73-963b-e8f39c8ae6da`。数据由
`profile_codex_session.py` 从 rollout JSONL 计算；本报告不包含原始提示词、命令或工具
输出。

## 结论

- 会话跨度：63,753.945 秒（17.71 小时）。
- 活跃任务口径：37,899.151 秒（10.53 小时）。
- 人工跨任务空闲：25,854.794 秒，已排除在活跃任务分母之外。
- 工具/后台可观测并集：24,938.768 秒，占活跃任务 65.80%。
- 模型/API/编排/未归因残差：12,960.383 秒，占 34.20%；这是减法残差，不能仅凭
  JSONL 继续归因为推理时间。

最大可观测耗时是子代理等待：398 次、23,226.365 秒，占活跃任务 61.28%。命令等待
393.272 秒；远程/加速器命令 284.928 秒；GitHub/外部协作 227.347 秒；测试/构建
197.363 秒。文件编辑仅 9.888 秒，因此本会话不是编辑或测试主导，而是父任务 #39 的
大量隔离票、NPU 队列和多轮 reviewer 收敛造成的子代理等待主导。

## 模型与上下文

- 配置：`gpt-5.6-sol/high`
- 请求：1,270 次
- compaction：6 次
- 输入 token：199,434,339；缓存输入 197,429,760；缓存率 98.99%
- 单请求输入 p50 162,501.5，p90 216,864.9，最大 233,262
- 输出 token：245,865

虽然缓存率很高，长会话仍承担大量请求处理与编排成本。当前中文报告增量本身只占整个
父任务会话后段；报告开发的主要实耗来自 TDD、两次完整套件和 Standards/Spec 多轮复审，
没有新增 NPU 运行。

## 后续改进

1. 对多个 issue 的长期父任务，在 frontier 批次完成后切分新的短会话，减少 16–23 万
   token 级上下文的重复请求处理。
2. reviewer 首轮前使用中文报告规范 checklist 预检 mandatory provenance、完整 HTML
   固定结构和协调篡改，减少定向复审轮次。
3. 保留“一次完整套件 + focused delta tests”的节奏；本轮最终两个 verifier hardening
   只需 focused/affected 回归，不应无证据重复完整套件。
4. 若需要把 34.20% 残差继续拆分，应接入 OpenTelemetry 或模型网关 trace；不得从
   rollout JSONL 主观推断服务排队或推理占比。
