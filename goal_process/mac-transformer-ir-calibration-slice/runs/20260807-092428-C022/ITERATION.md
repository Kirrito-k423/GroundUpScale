# C022：授权停止 board 服务并恢复测量资格

- **开始/结束：** 2026-08-07T09:24:28+08:00 / 2026-08-07T09:28:19+08:00
- **阶段：** BLOCKED -> PROBE
- **动作类型：** PROBE
- **关联验收/未知量：** AC-06、AC-07、AC-08、H-06

## 预注册

- **本轮 micro-goal：** 精确停止实际监听 8765/8766 的两个 autoresearch board 服务，等待系统负载回落，并以 `local-apple-silicon-v1` preflight PASS 证明 Goal 可恢复。
- **当前假设：** 停止两个活跃 listener 后，历史 0% CPU 非监听进程不会影响门禁；`mediaanalysisd` 当前已空闲，1-minute load 将在数分钟内回落。
- **已有证据：** C021；用户于 2026-08-07 明确授权“临时停止 board 服务并继续”；只读进程表显示活跃 PID 仍为 18974/18975，另有多组 0% CPU 同命令历史进程。
- **证据等级：** E3（board 服务是可观测竞争源）；恢复结果待探针。
- **唯一主要变量：** 正常终止实际监听 8765/8766 的服务实例；不停止历史非监听进程、不强杀系统服务、不改变门禁。
- **预期观察：** listener 经 SIGTERM 在 10 秒内退出，端口不再监听；随后 preflight 的竞争进程与 normalized load 均通过。
- **判别规则：** 先用 `lsof` 确认 PID、端口和命令双重匹配；只向匹配 PID 发送 SIGTERM。若未退出，不自动 SIGKILL。preflight exit 0 才进入新 cohort。
- **成本与风险：** 预计 1–10 分钟；board Web 页面暂不可用。保留原命令，完成测量后恢复两个服务并做端口健康检查。
- **停止与回滚：** PID/命令/端口任一不匹配则不停止；测量完成或本轮终止时，使用原参数恢复且避免产生重复 listener。

## 执行

- **脱敏命令：** `commands.md`
- **日志/指标：** `lsof` 确认 8765→18974、8766→18975；SIGTERM 后 18975 退出、两个 listener 均关闭；18974 40 秒后仍存活，CPU 在 0.4%–18.3% 间波动。首次 post-stop preflight：load `0.3033>0.25`、18974 `25.6%>25%`。

## 结果

- **观察事实：** 命令/端口/PID 双重匹配正确；未影响历史非监听进程。8766 完整退出，8765 停止对外服务但进程退出清理卡住。
- **错误签名：** `graceful-shutdown-timeout + PID 18974 + board serve 8765 + listener closed/process alive`。
- **推断：** SIGTERM 已产生预期的端口关闭，但挂载另一个 board 的实例没有完成进程退出；继续等待没有单调改善 CPU/load。
- **证据等级变化：** “两个 listener 可安全停止”E3；“SIGTERM 能让两个进程都退出”被反证。
- **信息增量：** 阻塞从两个活跃服务缩小到一个无 listener、可识别的残留进程。

## 结论

- **验收/交付更新：** 尚未解除 H-06；未启动 benchmark。
- **预算变化：** 约 4 分钟，无付费资源。
- **下一 micro-goal：** 对 18974 使用可逆 SIGSTOP，验证 CPU 归零并等待 load 回落。
- **是否需决策：** 用户已授权临时停止；不需要扩大到 SIGKILL。
