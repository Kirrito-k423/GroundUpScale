# C019 脱敏命令记录

```sh
uv lock --check
uv run pytest -q
bash -n scripts/run-local-m4-evidence.sh
git diff --check
git clone <public-repository> <temporary-directory>
uv sync --locked --group dev
uv run pytest -q
uv run groundupscale compile <plan> <two-output-directories>
diff -ru <first> <second>
gh run list / gh run view
git ls-remote origin refs/heads/main
```
